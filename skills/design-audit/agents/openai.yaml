interface:
  display_name: "Design Audit"
  short_description: "Audit UI against repo rules"
  default_prompt: "Use $design-audit to validate visual alignment and component architecture."
policy:
  allow_implicit_invocation: true
